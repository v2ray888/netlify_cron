name: Vercel Cron Trigger

on:
  schedule:
    # 每5分钟运行一次。请注意，GitHub Actions的schedule事件不保证精确到每分钟，可能会有几分钟的延迟。
    - cron: '*/5 * * * *'

jobs:
  trigger-cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel Cron API
        run: curl -X GET "https://您的Vercel应用域名.vercel.app/api/cron"